ENV=dev
# H2 is not natively supported from Python.
# For local dev, we use SQLite; for CI/CD or prod, use MySQL.
DATABASE_URL=sqlite:///./dev.db
# Example MySQL URL:
# DATABASE_URL=mysql+pymysql://user:password@localhost:3306/interest_db
